#include <stdio.h>
#include <stdbool.h>

int withdraw(int amount) {
    /// ...
}

int main() {
    bool condition = true;

    while(condition) {
        withdraw(0);
        // update condition
    }

    return 0;
}
